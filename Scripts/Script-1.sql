SELECT * FROM EMP;

SELECT DISTINCT DEPTNO 
FROM EMP;

SELECT 
DISTINCT JOB, DEPTNO
FROM EMP;

SELECT
ALL JOB, DEPTNO
FROM EMP;

SELECT 
ENAME, SAL, SAL *12 +COMM,
COMM
FROM EMP;



SELECT 
ENAME, SAL, 
SAL *12 + COMM AS "ANNSAL",
COMM
FROM EMP
WHERE SAL > 2000 AND SAL < 4000
ORDER BY SAL DESC;

SELECT 
ENAME, SAL, 
SAL *12 + COMM AS "ANNSAL",
COMM
FROM EMP
WHERE BETWEEN SAL 2000 AND 4000
ORDER BY SAL DESC;


SELECT * FROM EMP;

SELECT * 
FROM EMP
WHERE ENAME LIKE '%S%';










